const DEFAULT = 'default'
const DARK = 'dark'
const LIGHT = 'light'
const GOLD = 'gold'

export const imageSuffixByTheme = {
  [DEFAULT]: '-light',
  [LIGHT]: '',
  [DARK]: '-light',
}

export default {
  DEFAULT,
  DARK,
  GOLD,
  LIGHT,
}
